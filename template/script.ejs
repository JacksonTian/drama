V5.registerCard("<%=name%>", function () {
  console.log("<%=name%> card");

  var initialize = function () {
    var card = this;
    var view = V5.View(card.node);
    // TODO
  };

  return {
    initialize : initialize
  };
});
